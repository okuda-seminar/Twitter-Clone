title: GetReverseChronologicalHomeTimelineResponse
type: array
description: Response when fetching timeline items for reverse chronological home timeline.
items:
  oneOf:
    - $ref: '../schemas/post.yml'
    - $ref: '../schemas/repost.yml'
    - $ref: '../schemas/quote_repost.yml'
